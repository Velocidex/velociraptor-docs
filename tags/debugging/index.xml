<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Debugging on Velociraptor - Digging deeper!</title>
    <link>https://docs.velociraptor.app/tags/debugging/</link>
    <description>Recent content in Debugging on Velociraptor - Digging deeper!</description>
    <generator>Hugo</generator>
    <language>en-us</language>
    <lastBuildDate>Wed, 03 Jun 2026 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://docs.velociraptor.app/tags/debugging/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>GRPC error, received message larger than max</title>
      <link>https://docs.velociraptor.app/knowledge_base/tips/grpc_errors/</link>
      <pubDate>Wed, 03 Jun 2026 00:00:00 +0000</pubDate>
      <guid>https://docs.velociraptor.app/knowledge_base/tips/grpc_errors/</guid>
      <description>&lt;h1 id=&#34;grpc-error-received-message-larger-than-max&#34;&gt;GRPC error, received message larger than max&lt;/h1&gt;&#xA;&lt;p&gt;This error occurs when an API call attempts to transfer a message&#xA;larger than the maximum allowed (by default about 4Mb).&lt;/p&gt;&#xA;&lt;p&gt;Velociraptor&amp;rsquo;s GUI uses the gRPC API internally to interact with the&#xA;server. When the API call attempts to fetch too much data the gRPC&#xA;backend will refuse the call with the above error.&lt;/p&gt;&#xA;&lt;h2 id=&#34;what-does-it-mean&#34;&gt;What does it mean?&lt;/h2&gt;&#xA;&lt;p&gt;The error &lt;code&gt;GRPC Error: received message larger than max&lt;/code&gt; occurs when&#xA;an API call returns too much data to fit within a single gRPC&#xA;call.&lt;/p&gt;&#xA;&lt;h3 id=&#34;can-i-increase-the-maximum-size-of-the-grpc-call&#34;&gt;Can I increase the maximum size of the gRPC call?&lt;/h3&gt;&#xA;&lt;p&gt;It is possible to change the gRPC message size limit using the&#xA;configuration file. Simply add the following section.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-yaml&#34;&gt;api_config:&#xA;  max_grpc_recv_size: 4000000&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;However, this not recommended! The default 4Mb limit is reasonable and&#xA;increasing it will lead to performance degradation. Generally messages&#xA;should be kept small for performance reasons so it is better to fix&#xA;the root cause rather than just increase the limit.&lt;/p&gt;&#xA;&lt;h3 id=&#34;poorly-written-vql-query&#34;&gt;Poorly written VQL query.&lt;/h3&gt;&#xA;&lt;p&gt;Most often the error occurs when the VQL query within an artifact&#xA;attempts to return too much data within the JSON output. This is&#xA;common for example with use of the&#xA;&lt;a href=&#34;https://docs.velociraptor.app/vql_reference/popular/read_file/&#34;&gt;read_file()&lt;/a&gt; function. While VQL&#xA;itself has no problem stuffing any size object into a single row, the&#xA;GUI in unable to fetch such a large row.&lt;/p&gt;&#xA;&lt;p&gt;A common pattern is:&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-vql&#34;&gt;SELECT OSPath, read_file(filename=OSPath) AS FileData&#xA;FROM yara(....)&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;The above query attempts to read the file that matched and pass it&#xA;back as field. This query is likely to fail because the row will be&#xA;too large and may exceed the default 4mb gRPC transfer limit when the&#xA;GUI attempts to read it.&lt;/p&gt;&#xA;&lt;p&gt;The above query may be rewritten as:&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-vql&#34;&gt;SELECT OSPath, upload(file=OSPath) AS Upload&#xA;FROM yara(...)&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;This query is much better as the bulk data is tranferred as a file and&#xA;only a reference is emitted into the JSON. Within an artifact you may&#xA;specify that the column be viewed with the &lt;code&gt;upload_preview&lt;/code&gt; column&#xA;type which will render a nice inspector (with a hex viewer, text,&#xA;allow searching within the blob etc).&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-yaml&#34;&gt;name: TestArtifact&#xA;sources:&#xA;- query: |&#xA;  SELECT OSPath, upload(file=OSPath) AS Upload&#xA;  FROM yara(...)&#xA;&#xA;column_types:&#xA;- name: Upload&#xA;  type: upload_preview&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h3 id=&#34;parameters-too-large-for-collection&#34;&gt;Parameters too large for collection&lt;/h3&gt;&#xA;&lt;p&gt;Some artifacts accept parameters which can potentially be very&#xA;large. For example, consider the&#xA;&lt;a href=&#34;https://docs.velociraptor.app/artifact_references/pages/generic.detection.yara.glob/&#34;&gt;Generic.Detection.Yara.Glob&lt;/a&gt;&#xA;artifact. This artifact accepts Yara rules as a parameters.&lt;/p&gt;&#xA;&lt;p&gt;If you have very large rule set (potentially over 4mb) then the&#xA;request Velociraptor needs to send to the client becomes very&#xA;large. This may cause a gRPC error when viewing the request in the&#xA;GUI.&lt;/p&gt;&#xA;&lt;p&gt;In this case it is better to send the Yara ruleset as an &lt;a href=&#34;https://docs.velociraptor.app/docs/artifacts/tools/&#34;&gt;external&#xA;tool&lt;/a&gt; or specify the parameter type as an&#xA;&lt;code&gt;upload&lt;/code&gt;. The above artifact already supports accepting Yara rules via&#xA;these methods. External tools or uploads are fetched separately by the&#xA;endpoint over HTTP. The resulting request is kept small and the server&#xA;is more efficient.&lt;/p&gt;&#xA;&#xA;&lt;div class=&#34;mynotices warning&#34;&gt;&#xA;  &lt;div heading=&#34; Hard size limit imposed &#34;&gt;&lt;p&gt;From version 0.77, Velociraptor imposes a hard size limit on flow,&#xA;hunt or client request objects. This means that launching collections&#xA;with very large parameters will be rejected.&lt;/p&gt;&#xA;&lt;p&gt;Although it is possible to adjust this limit using the&#xA;&lt;a href=&#34;https://docs.velociraptor.app/docs/deployment/references/#Datastore.max_object_size&#34;&gt;Datastore.max_object_size&lt;/a&gt;&#xA;parameter, it is better to identify the reason why datastore objects&#xA;are too large.&lt;/p&gt;&#xA;&lt;p&gt;By rejecting the scheduling of very large collections, we remove&#xA;performance issues before they even occur.&lt;/p&gt;&#xA;&lt;/div&gt;&#xA;&lt;/div&gt;&#xA;&#xA;&#xA;</description>
    </item>
    <item>
      <title>How do I debug the client while it is running?</title>
      <link>https://docs.velociraptor.app/knowledge_base/tips/debugging_clients/</link>
      <pubDate>Mon, 21 Mar 2022 00:00:00 +0000</pubDate>
      <guid>https://docs.velociraptor.app/knowledge_base/tips/debugging_clients/</guid>
      <description>&lt;h1 id=&#34;how-do-i-debug-the-client-while-it-is-running&#34;&gt;How do I debug the client while it is running?&lt;/h1&gt;&#xA;&lt;p&gt;Sometimes we collect artifacts from clients but for some reason things seem to take longer than expected. Velociraptor has mechanisms to gain visibility into how clients behave and what queries are running.&lt;/p&gt;&#xA;&lt;h2 id=&#34;query-logs&#34;&gt;Query logs&lt;/h2&gt;&#xA;&lt;p&gt;The first port of call is viewing the query logs in the logs tab of the relevant collection.&lt;/p&gt;&#xA;&lt;p&gt;&#xA;&#xA;&#xA;&#xA;&#xA;&#xA;&#xA;&lt;figure&gt;&#xA;  &lt;div class=&#34;figure&#34;&gt;&#xA;    &lt;img src=&#34;https://docs.velociraptor.app/knowledge_base/tips/debugging_clients/https://user-images.githubusercontent.com/3856546/159195874-ec6c8322-cb70-4254-861a-95888db94201.png&#34;&#xA;         class=&#34;inline&#34;&#xA;         alt=&#34;Viewing the query logs&#34; /&gt;&#xA;  &lt;/div&gt;&#xA;  &lt;figcaption&gt;&#xA;    Viewing the query logs&#xA;  &lt;/figcaption&gt;&#xA;&lt;/figure&gt;&#xA;&#xA;&#xA;&lt;/p&gt;&#xA;&lt;p&gt;As the query is running, it will emit a message to let us know that it is waiting for rows. We use this to determine that the query is still running on the client.&lt;/p&gt;&#xA;&lt;h2 id=&#34;collecting-profiles&#34;&gt;Collecting profiles&lt;/h2&gt;&#xA;&lt;p&gt;The &lt;code&gt;Generic.Client.Profile&lt;/code&gt; artifact allows us to collect internal state of the client. Simply collect this from the client, while other queries are running&lt;/p&gt;&#xA;&lt;p&gt;&#xA;&#xA;&#xA;&#xA;&#xA;&#xA;&#xA;&lt;figure&gt;&#xA;  &lt;div class=&#34;figure&#34;&gt;&#xA;    &lt;img src=&#34;https://docs.velociraptor.app/knowledge_base/tips/debugging_clients/https://user-images.githubusercontent.com/3856546/159196011-28808471-4111-42ba-bfd3-819381bdf596.png&#34;&#xA;         class=&#34;inline&#34;&#xA;         alt=&#34;Client Profile&#34; /&gt;&#xA;  &lt;/div&gt;&#xA;  &lt;figcaption&gt;&#xA;    Client Profile&#xA;  &lt;/figcaption&gt;&#xA;&lt;/figure&gt;&#xA;&#xA;&#xA;&lt;/p&gt;&#xA;&lt;p&gt;The most common thing to collect include:&lt;/p&gt;&#xA;&lt;ol&gt;&#xA;&lt;li&gt;The Goroutine dump shows a stack trace of all currently running goroutines (similar to threads). This helps us understand if there is a deadlock or another bug.&lt;/li&gt;&#xA;&lt;li&gt;The logs delivers a recent dump of client logs. Normally the client does &lt;strong&gt;not&lt;/strong&gt; write it&amp;rsquo;s logs to file to avoid information leakage issues. You can see the logs on the console by running the client with the &lt;code&gt;-v&lt;/code&gt; flag, but each client also keeps the last 1000 messages in a memory buffer so they can be available if needed. This option sends the recent logs to the server.&lt;/li&gt;&#xA;&lt;li&gt;Query logs are a recent log of VQL queries running on the endpoint. This gives us an idea of exactly what the client is doing.&lt;/li&gt;&#xA;&lt;li&gt;Metrics are internal program counters that provide visibility of performance related items.&lt;/li&gt;&#xA;&lt;/ol&gt;&#xA;&lt;p&gt;When asking for help on Discord or our mailing list, we will often ask for the profiles collected from the client (or server). At a minimum we will need the above items to diagnose any issues.&lt;/p&gt;&#xA;&#xA;&lt;div class=&#34;mynotices tip&#34;&gt;&#xA;  &lt;div heading=&#34;tip&#34;&gt;&lt;p&gt;The nice thing about collecting profiles is that the client does &lt;strong&gt;not&lt;/strong&gt; need to be restarted and we do not need to run a special debug build - all clients are capable of collecting profile information at any time.&lt;/p&gt;&#xA;&lt;/div&gt;&#xA;&lt;/div&gt;&#xA;&#xA;&#xA;&lt;h3 id=&#34;references&#34;&gt;References&lt;/h3&gt;&#xA;&lt;p&gt;You can read more about &lt;a href=&#34;https://docs.velociraptor.app/blog/2020/2020-08-16-profiling-the-beast-58913437fd16/&#34;&gt;profiling Velociraptor here&lt;/a&gt;.&lt;/p&gt;&#xA;</description>
    </item>
  </channel>
</rss>
